*** Begin Patch
*** Update File: app/courses/page.tsx
@@
-import { motion, AnimatePresence } from 'framer-motion'
+import { motion, AnimatePresence } from 'framer-motion'
@@
-const itemVariants = {
-  hidden: { opacity: 0, y: 6 },
-  show: { opacity: 1, y: 0, transition: { duration: 0.3, ease: 'cubic-bezier(0.25, 0.46, 0.45, 0.94)' } },
-}
-
-// Motion-enhanced Grid item so AnimatePresence can control exit animations
-const MotionGridItem = motion(Grid)
+const itemVariants = {
+  hidden: { opacity: 0, y: 6 },
+  show: {
+    opacity: 1,
+    y: 0,
+    transition: { duration: 0.3, ease: [0.25, 0.46, 0.45, 0.94] },
+  },
+}
@@
-              {filtered.map((course) => (
-                <MotionGridItem
+              {filtered.map((course) => (
+                <Grid
                   key={course.id}
                   item
                   xs={12}
                   sm={6}
-                  variants={itemVariants}
-                  initial="hidden"
-                  animate="show"
-                  exit={{ opacity: 0, y: 4 }}
+                  component={motion.div}
+                  variants={itemVariants}
+                  initial="hidden"
+                  animate="show"
+                  exit={{ opacity: 0, y: 4 }}
                 >
                   <Card sx={{ height: '100%' }}>
@@
-                </MotionGridItem>
+                </Grid>
               ))}
*** End Patch